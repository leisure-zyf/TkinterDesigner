Construct a label widget with the parent MASTER.
STANDARD OPTIONS:
    activebackground, activeforeground, anchor, background, bitmap, borderwidth, 
    cursor, disabledforeground, font, foreground, highlightbackground, highlightcolor, 
    highlightthickness, image, justify, padx, pady, relief, takefocus, text, textvariable, 
    underline, wraplength
WIDGET-SPECIFIC OPTIONS:
    height, state, width

使用父 MASTER 构造标签构件.
标准选项:
    活动背景, 活动前场, 锚点, 背景, 位图, 边框宽度, 光标, 
    禁用背景, 字体, 前景, 高亮显示背景, 高亮颜色,
    高亮显示, 图像, 对齐, padx, pady, 浮雕, 获取焦点, 文本, 文本可变,
    下划线, 换行长度
小部件特定的选项:
    高度,状态,宽度



Construct a button widget with the parent MASTER.
STANDARD OPTIONS:
    activebackground, activeforeground, anchor, background, bitmap,
    borderwidth, cursor, disabledforeground, font, foreground 
    highlightbackground, highlightcolor, highlightthickness,
    image, justify, padx, pady, relief, repeatdelay, repeatinterval,
    takefocus, text, textvariable, underline, wraplength
WIDGET-SPECIFIC OPTIONS:
    command, compound, default, height, overrelief, state, width

使用父 MASTER 构造按钮小部件.
标准选项:
    活动背景, 活动前瞻, 锚点, 背景, 位图, 边框宽度, 光标,
    禁用前景, 字体, 前景高亮显示背景, 高亮显示, 高亮显示,
    图像, 对齐, padx, pady, 浮雕, 重复透视, 重复间隔,
    拍摄焦点, 文本, 文本变量, 下划线, 换行长度
小部件特定的选项:
    命令, 复合, 默认值, 高度, 过度, 状态, 宽度